BRAND=Liaison
DB_PATH=liaison.db

# WhatsApp concierge number people message after signup (Wassist inbound handoff).
# Use YOUR Wassist WhatsApp Business number once provisioned. Digits only, incl. country code.
CONCIERGE_WA_NUMBER=447883319107

# Hermes matchmaking brain (local via Ollama, OpenAI-compatible)
#   ollama pull hermes3
LLM_BASE_URL=http://localhost:11434/v1
LLM_MODEL=hermes3:8b
LLM_API_KEY=ollama

# PayPal — UNLOCK step only (downstream of matching)
PAYPAL_ENVIRONMENT=SANDBOX
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
UNLOCK_PRICE=1.00
UNLOCK_CURRENCY=GBP
